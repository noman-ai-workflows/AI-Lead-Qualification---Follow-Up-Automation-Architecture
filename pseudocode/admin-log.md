function persistLatestExecutionLog(log):
overwrite in-memory store with latest execution

function getLatestExecutionLog():
return last stored log

<!-- In production, logs would be persisted to a database or observability system. -->
